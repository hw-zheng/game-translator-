# AI Galgame 自动化汉化软件设计方案

## 1. 概述 (Overview)

基于用户提供的文件结构（特别是 `.pfs` 文件和 `hamidashi.exe`），可以判断该游戏极有可能使用的是 **Artemis Engine**（或者与其结构相似的引擎，如 WillPlus 的系统）。

设计一款能够自动解包、通过 AI API 翻译、并重新打包的软件是**完全可行**的，但需要解决文件逆向、文本解析、AI 上下文理解和字体适配等技术难点。

## 2. 目标文件结构分析 (Target Analysis)

从提供的文件列表来看：
*   `hamidashi.exe`: 游戏主程序。
*   `hamidashi.pfs*`: 资源归档文件。通常包含脚本 (.ast/.txt)、图片 (.png/.tlg)、音频 (.ogg) 等。
    *   **关键点**: 需要解包这些文件以获取文本脚本。
*   `font/`: 存放字体文件。
    *   **关键点**: 原版游戏通常使用日语字体（Shift-JIS 编码），汉化需要替换为支持 GBK/UTF-8 的中文字体，并可能需要修改 EXE 或配置以加载新字体。

## 3. 软件架构设计 (Software Architecture)

软件整体分为四个核心模块：

### 3.1 资源管理模块 (Asset Manager) - "解包与封包"
负责处理 `.pfs` 等特定格式的归档文件。
*   **解包器 (Unpacker)**:
    *   识别 `.pfs` 文件头和加密算法（Artemis 引擎通常有公开的解包工具源码参考，如 GARBro）。
    *   将 `.pfs` 中的文件提取到临时目录。
*   **封包器 (Repacker)**:
    *   将修改后的脚本和资源重新打包成游戏可读取的 `.pfs` 格式（或生成补丁文件让游戏优先读取）。

### 3.2 脚本处理模块 (Script Processor) - "理解与还原"
这是最复杂的部分，因为 AI 不能直接翻译带有代码的脚本。
*   **反编译/解析 (Parser)**:
    *   如果脚本是编译过的二进制（.ast），需要反编译为文本。
    *   **提取文本**: 从脚本中分离出“对白”和“代码”。使用正则表达式识别控制符（如 `\n`, `%name%`, 颜色代码等）。
    *   **结构化**: 将提取的文本转换为中间格式（如 JSON/CSV），记录行号、说话人、原始文本、上下文信息。
*   **重组 (Reconstructor)**:
    *   将 AI 翻译后的文本回填到原始脚本结构中。
    *   **自动换行处理**: 很多 Galgame 引擎需要手动换行，需根据中文字体宽度自动插入换行符。

### 3.3 AI 翻译核心 (AI Translation Core) - "信达雅"
利用 LLM (GPT-4, Claude 3, DeepSeek 等) 进行翻译。
*   **API 客户端**: 管理与 AI 的连接。
*   **Prompt 工程**:
    *   **系统提示词**: 设定 AI 为“专业 Galgame 汉化组”，要求保留原文语气。
    *   **术语表 (Glossary)**: 建立角色名、地名、专有名词的映射表，确保翻译一致性。
    *   **上下文记忆**: 将前几句对话作为 Context 输入，避免 AI 不知道代词指代对象（例如日语中常省略的主语）。
*   **并发处理**: 多线程发送请求以提高速度。

### 3.4 补丁注入与运行时 (Injector & Runtime)
*   **字体 Hook**: 如果游戏内核不支持中文编码（如仅支持 Shift-JIS），可能需要通过 DLL 注入（Hook TextOut 等 API）来动态替换字符编码或字体，或者修改 EXE 的 Hex 数据。
*   **本地化文件生成**: 生成最终的 `.pfs` 补丁或覆盖文件。

## 4. 工作流 (Workflow)

1.  **初始化**: 用户选择游戏根目录 (`D:\...\hamidashi`).
2.  **分析**: 软件扫描 `.pfs` 文件，识别引擎版本。
3.  **提取**: 解包所有 `.pfs` 文件，过滤出脚本文件。
4.  **预处理**: 解析脚本，提取纯文本，建立 `Original -> Placeholder` 的映射。
5.  **翻译**:
    *   用户输入 OpenAI/Claude API Key。
    *   软件自动提取角色名，先让 AI 生成“角色名对照表”供用户确认。
    *   批量发送文本进行翻译，实时显示进度。
6.  **后处理**:
    *   文本回填。
    *   自动处理过长文本的换行。
    *   字体文件替换/生成。
7.  **输出**: 生成 `hamidashi_cn.pfs` 或覆盖原文件，并生成“启动汉化版.bat”。

## 5. 技术难点与解决方案

| 难点 | 解决方案 |
| :--- | :--- |
| **特殊码表 (Shift-JIS)** | Artemis 引擎较老版本可能不支持 UTF-8。方案一：将汉字转为 Shift-JIS 兼容编码（但这会缺字）。方案二（推荐）：修改 EXE 汇编代码，使其支持 GBK 或 UTF-8；或者使用像 BGI/Artemis 通用的 Hook 工具。 |
| **控制符干扰** | 在发送给 AI 前，将 `\k`, `%1` 等控制符替换为 `[VAR1]` 等占位符，翻译后再还原。 |
| **上下文缺失** | 采用“滑动窗口”策略，每次发送当前句 + 前 5 句历史记录给 AI。 |
| **H 场景/敏感词** | 使用支持 NSFW 内容的模型，或在 Prompt 中明确“这是文学创作”。 |

## 6. 结论

**完全可行。** 这个项目本质上是一个 **"Artemis 引擎专用 AI 汉化流水线"**。

由于您已经有了手动汉化的文件结构（看到了 `汉化补丁` 文件夹），这证明该游戏的解包和封包技术路线已经跑通。我们要做的就是用“AI + 自动化脚本”替代“人工翻译 + 手动导入导出”的过程。
